extends layouts/default.pug
include mixins/box-head.pug
block main
   +box-head("Danh sách sản phẩm", "Đây là đoạn mô tả của trang danh sách sản phẩm")


   ul 
    each item in products1
        li #{item.title}
        li #{item.price}
        li #{item.thumbnail}